# Decisions

One short file per decision that isn't recoverable from the code or the API
snapshots — mostly "we couldn't port X from Sleeper, here's what we did instead".

Format: `NNN-short-title.md`, with **Context / Decision / Consequence**.
